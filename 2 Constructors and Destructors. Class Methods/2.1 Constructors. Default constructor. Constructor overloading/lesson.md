# 2.1 Конструкторы. Конструктор по умолчанию. Перегрузка конструкторов

## Введение

Итак, на предыдущем занятии у нас с вами получился следующий класс (если записать его в укороченном виде):

```cpp
class Point2D {
public:
    void set_coords(int a, int b) { 
        x = a; 
        y = b; 
    }

    void get_coords(int& a, int& b) { 
        a = x; 
        b = y; 
    }

private:
    int x, y;
};
```

И мы знаем, что объекты этого класса могут создаваться либо так:

```cpp
Point2D pt;
```

либо так:

```cpp
Point2D* ptr_pt = new Point2D;
```

И здесь возникает один важный тонкий момент. После создания объекта класса `Point2D` мы ничего не можем сказать о его внутреннем состоянии, то есть, о значениях переменных `x` и `y`. Получается, что созданные объекты находятся в неопределённом состоянии. Это нарушает одно из положений ООП:

> **Программист не должен делать каких-либо предположений о внутреннем состоянии объекта. Поведение любого объекта должно быть предсказуемым.**

Класс `Point2D` явно нарушает это правило. Если вывести значения координат созданных объектов:

```cpp
int main() {
    Point2D pt;
    Point2D* ptr_pt = new Point2D;

    int x, y;

    pt.get_coords(x, y);
    std::cout << x << " " << y << std::endl;

    ptr_pt->get_coords(x, y);
    std::cout << x << " " << y << std::endl;

    delete ptr_pt;

    return 0;
}
```

то увидим в консоли неопределённые значения. Как же это исправить? Очень просто! После размещения нового объекта в памяти устройства всегда автоматически вызывается специальный метод, который называется **конструктор**. И в этом методе, как раз, часто выполняется установка начального состояния нового созданного объекта.

## Свойства конструкторов

Конструкторы обладают следующими свойствами:

- имя конструктора всегда должно совпадать с именем типа данных, в нашем случае с именем класса `Point2D`;
- конструктор никогда не возвращает никаких значений, поэтому возвращаемый тип **не прописывается**;
- конструктор может иметь произвольное число параметров;
- конструктор всегда вызывается при создании каждого нового объекта.

Учитывая всё это, объявим конструктор в классе `Point2D` следующим образом:

```cpp
class Point2D {
public:
    Point2D() : x{0}, y{0} {}

    void set_coords(int a, int b) { 
        x = a; 
        y = b; 
    }

    void get_coords(int& a, int& b) { 
        a = x; 
        b = y; 
    }

private:
    int x, y;
};
```

## Список инициализации

Обратите внимание, как выполняется инициализация полей `x`, `y` при вызове конструктора. После его определения, но перед телом конструктора, ставится двоеточие и через запятую прописывается инициализация переменных `x` и `y` нулевыми значениями. И это **рекомендуемая практика**. Такая инициализация гарантирует, что в теле конструктора переменные `x`, `y` будут принимать нужные нам начальные значения.

Кроме того, в списке инициализации переменные следует прописывать **в порядке их объявления** в классе. У нас сначала указана переменная `x`, а затем - переменная `y`. Именно поэтому в блоке инициализации в конструкторе переменные идут в том же порядке.

Наконец, список инициализации - это **единственный способ** инициализации константных переменных непосредственно в конструкторе. Например:

```cpp
class Point2D {
public:
    Point2D() : max_coord{100}, x(0), y(0) {}

private:
    const unsigned max_coord;
    int x, y;
};
```

После определения конструктора без параметров, при создании каждого нового объекта класса `Point2D` будем иметь нулевые значения координат. В результате мы с вами устранили неопределённость состояния создаваемых объектов.

## Инициализация полей при объявлении

Помимо использования списка инициализации в конструкторах, имеется возможность указывать инициализацию переменных непосредственно в классе при их объявлении. Например, так:

```cpp
class Point2D {
    int x{0}, y{0};
    // ...
};
```

И это **рекомендуемая практика**, которая гарантирует нужные нам начальные значения и, как следствие, начальное состояние объекта, даже если по каким-либо причинам в классе не будет явно определено ни одного конструктора.

Общее правило для **Списка инициализации** и для **Инициализации полей при объявлении** в том, что значения по умолчанию - в объявлении полей, логику - в конструкторе, то есть желательно оба варианта в одном классе.

```cpp
class Point2D {
public:
    Point2D() = default;
    Point2D(int a, int b) : x{a}, y{b} {}

private:
    int x{0}, y{0};
};
```

## Конструктор по умолчанию

В концепции ООП конструкторы, которые можно вызывать без параметров, называются **конструкторами по умолчанию**. В частности, конструктор, объявленный в классе `Point2D`, является таким. Но мы можем переписать этот же конструктор с двумя параметрами и значениями по умолчанию, например:

```cpp
class Point2D {
public:
    Point2D(int a = 0, int b = 0) : x{a}, y{b} {}

private:
    int x, y;
};
```

который также будет считаться конструктором по умолчанию, так как мы можем его вызывать, не передавая никаких аргументов.

Почему акцентируется внимание на такой тип конструкторов? Дело в том, что в языке C++ им отведена своя особая роль. Мы уже видели, что такой конструктор срабатывает всякий раз, когда объект создаётся обычным образом, без указания каких-либо дополнительных аргументов. Также этот тип конструктора вызывается при создании массива объектов. Например:

```cpp
Point2D ar_pt[5];
```

Здесь создаётся пять объектов класса `Point2D` и для каждого автоматически вызывается конструктор по умолчанию.

Внимательный слушатель сейчас может задаться вопросом, а как создаются объекты класса `Point2D` без явного объявления конструктора в классе? На самом деле в любом классе всегда имеется конструктор и, как мы увидим позже, не один. Если мы не прописываем свой собственный, то компилятор автоматически создаёт конструктор по умолчанию без параметров и с пустым телом, который не выполняет никаких действий и нужен лишь для сохранения общей логики создания объектов.

## Перегрузка конструкторов

Так как конструкторы могут иметь произвольные параметры, то в одном классе можно объявлять несколько конструкторов с разным набором параметров. Например, следующим образом:

```cpp
class Point2D {
public:
    Point2D() {}
    Point2D(int a, int b) : x{a}, y{b} {}

private:
    int x{0}, y{0};
};
```

Соответственно, если объект создаётся без указания аргументов:

```cpp
Point2D* ptr_pt = new Point2D;
```

то вызывается конструктор по умолчанию (без параметров). А если объект создаётся с двумя аргументами:

```cpp
Point2D pt(1, 2);
```

то вызывается конструктор с двумя параметрами. Компилятор именно по типу и числу аргументов выбирает тот или иной конструктор при формировании нового объекта. Сами же конструкторы становятся перегруженными.

Благодаря наличию двух конструкторов в классе `Point2D`, его объекты можно создавать двумя способами: без аргументов и с указанием двух аргументов. В этом удобство механизма перегрузки конструкторов класса. Но, как мы увидим дальше, перегрузка конструкторов необходима не только для удобства создания объектов в том или ином виде, но и для обеспечения стандартных операций с объектами класса, например, копирования.

---

## Тест

### Вопрос 1

Выберите все верные утверждения, касающиеся конструкторов классов.

- [ ] конструктор всегда вызывается при удалении объекта
- [ ] конструктор никогда не возвращает никаких значений, поэтому возвращаемый тип прописывается как `void`
- [x] конструктор никогда не возвращает никаких значений, поэтому возвращаемый тип не прописывается
- [x] имя конструктора всегда должно совпадать с именем класса
- [x] конструктор может иметь произвольное число параметров
- [x] конструктор всегда вызывается при создании каждого нового объекта

### Вопрос 2

Выберите все верные утверждения, касающиеся следующей программы:

```cpp
#include <iostream>

class Vector {
public:
    Vector() : x{0}, y{0} {}

private:
    int x, y;
};

int main() {
    Vector v;
    std::cout << v.x << " " << v.y << std::endl;

    return 0;
}
```

- [x] в конструкторе класса `Vector` выполняется начальная инициализация переменных `x`, `y` нулевыми значениями
- [x] класс `Vector` имеет конструктор, который называется конструктором по умолчанию
- [ ] объект класса `Vector` следует создавать командой `Vector v();`
- [x] при компиляции программы возникнет ошибка из-за прямого обращения к полям `x`, `y` через объект `v`
- [x] при создании объекта `v` автоматически вызывается объявленный конструктор класса

### Вопрос 3

Выберите все верные утверждения, касающиеся конструкторов классов.

- [x] в классе можно объявлять несколько конструкторов с разным набором параметров
- [x] конструктором по умолчанию называют любой конструктор, который можно вызывать без передачи в него аргументов
- [x] после прототипа конструктора через двоеточие можно прописывать начальную инициализацию переменных объекта класса
- [ ] конструктором по умолчанию называют конструктор, объявленный без параметров (и только такой)
- [ ] после прототипа конструктора через двоеточие прописывается начальная инициализация полей класса, а не создаваемого объекта

### Вопрос 4

Выберите все верные утверждения, касающиеся переменных (полей) и их инициализации.

- [x] в списке инициализации конструкторов переменные следует записывать в том же порядке, что и при их объявлении в классе
- [ ] начальную инициализацию переменных в классе при их объявлении прописывать нельзя
- [x] обычные (не статические) переменные, объявленные в классе, формируются (появляются) только в его объектах
- [x] начальную инициализацию переменных можно прописывать в классе при их объявлении
- [x] если прописана инициализация переменных и в классе и при вызове конструктора (в списке инициализации), то приоритет отдаётся списку инициализации

### Вопрос 5

Выберите все верные утверждения, касающиеся следующей программы:

```cpp
#include <iostream>

class Vector {
public:
    Vector() : x{0}, y{0} {}
    Vector(int a = 0, int b = 0) : x{a}, y{b} {}

    int x{-1}, y{-1};
};

int main() {
    Vector v, v2(1, 2);
    std::cout << v2.x << " " << v2.y << std::endl;

    return 0;
}
```

- [x] в случае успешного создания объекта `v2` значения его координат будут равны `x=1`, `y=2`
- [ ] в случае успешного создания объекта `v2` значения его координат равны `x=-1`, `y=-1`
- [x] оба конструктора класса `Vector` являются конструкторами по умолчанию
- [ ] только первый конструктор класса `Vector` можно назвать конструктором по умолчанию
- [x] при компиляции программы возникнет ошибка, т.к. при создании объекта `v` возникает неопределённость выбора конструктора